apiVersion: v1
kind: Service
metadata:
  name: code-runner-api
  namespace: code-runner-system
spec:
  type: ClusterIP
  selector:
    app: code-runner-api
  ports:
    - name: http
      port: 80
      targetPort: 8080
